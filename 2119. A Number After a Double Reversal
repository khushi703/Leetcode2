class Solution {
    public boolean isSameAfterReversals(int num) {
        int ans =0;
        ans = num%10;
        num/=10;
        if(ans==0 && num!=0)
        return false;
        else
        return true;
        
    }
}
